// miniz_export.h —— 由 miniz 的 amalgamate.sh 生成;仓库里没有该文件,
// 这里按生成版内容重建(仅导出宏,本库内部使用无需可见性控制)。
#pragma once

#ifndef MINIZ_EXPORT
#define MINIZ_EXPORT
#endif

#ifndef MINIZ_EXPORT_CPP
#define MINIZ_EXPORT_CPP
#endif